# 📱 راهنمای اتصال موبایل به VM های ESXi

## 🔌 روش اول: اتصال فیزیکی USB 3.1

### ⚠️ مشکل شناسایی شده

هنگام اتصال فیزیکی موبایل به پورت USB 3.1 سرور ، وی ام ویندوزی گوشی را شناسایی نمی کند

و در VM لینوکسی خطای زیر در ترمینال مشاهده می شود :

```
device descriptor read/64, error -71
```

این خطا معمولاً به دلیل محدودیت‌های USB Passthrough در ESXi رخ می‌دهد.

---

## 📡 روش دوم: اتصال بی‌سیم (Wireless ADB) - روش پیشنهادی

### مرحله ۱: آماده‌سازی گوشی 🔧

> **نکته:** این مراحل فقط یک بار انجام می‌شود.

1. **فعال‌سازی Developer Options:**
   - Settings → About Phone
   - روی **Build Number** هفت بار ضربه بزنید
   - پیام *"You are now a developer"* نمایش داده می‌شود

2. **فعال‌سازی USB Debugging:**
   - Settings → Developer Options
   - گزینه **USB Debugging** را فعال کنید

3. **اتصال به شبکه:**
   - گوشی را به همان شبکه Wi-Fi که سرور HP به آن متصل است وصل کنید
   - IP گوشی را یادداشت کنید (مثال: `192.168.1.50`)
   - محل یافتن IP: Settings → Wi-Fi → اطلاعات شبکه متصل

---

### مرحله ۲: آماده‌سازی کامپیوتر واسط 💻

> **نکته:** نیازی به نصب کامل Android Studio نیست!

1. **دانلود ADB Platform Tools:**
   - [دانلود SDK Platform-Tools از گوگل](https://developer.android.com/studio/releases/platform-tools)
   - حجم فایل کم است (حدود 10MB)

2. **استخراج و آماده‌سازی:**
   - فایل ZIP را در مسیری مانند `C:\adb-tools` استخراج کنید
   - وارد پوشه شوید (محل فایل `adb.exe`)
   - با نگه داشتن **Shift + کلیک راست** در پوشه، گزینه زیر را انتخاب کنید:
     - `Open PowerShell window here` یا
     - `Open Command window here`

---

### مرحله ۳: تنظیم حالت TCP/IP روی گوشی ⚙️

1. **اتصال کابلی موقت:**
   - گوشی را با کابل USB به کامپیوتر متصل کنید
   - قفل صفحه گوشی را باز کنید

2. **تایید USB Debugging:**
   - پیام *"Allow USB Debugging?"* روی گوشی ظاهر می‌شود
   - ✅ گزینه **Always allow from this computer** را تیک بزنید
   - دکمه **OK** را بزنید

3. **بررسی اتصال:**
   ```bash
   adb devices
   ```
   - خروجی صحیح: کد دستگاه + کلمه `device`
   - اگر `unauthorized` نوشت: پیام روی گوشی را تایید نکرده‌اید

4. **فعال‌سازی حالت TCP/IP:**
   ```bash
   adb tcpip 5555
   ```
   - پیام موفقیت: `restarting in TCP mode port: 5555`

5. **قطع کابل:**
   - ✨ گوشی اکنون در حالت بی‌سیم آماده است!

---

### مرحله ۴: اتصال نهایی در VM لینوکسی 🐧

در ترمینال VM لینوکسی روی ESXi:

```bash
adb connect 192.168.1.50:5555
```

> جایگزین کردن `192.168.1.50` با IP واقعی گوشی

**پیام موفقیت:**
```
connected to 192.168.1.50:5555
```

**بررسی اتصال:**
```bash
adb devices
```

خروجی باید IP گوشی را نشان دهد:
```
192.168.1.50:5555    device
```

---

## ⚡ نکات مهم و بحرانی

### 🔄 در صورت ریستارت گوشی
حالت TCP/IP از بین می‌رود و باید مجدداً:
- گوشی را با کابل به کامپیوتر واسط وصل کنید
- دستور `adb tcpip 5555` را اجرا کنید

**💡 راه‌حل برای Device Farm:** گوشی‌ها را همیشه روشن نگه دارید و از خاموش شدن خودکار جلوگیری کنید.

### 🔋 مدیریت باتری
- کابل USB قطع شده است → گوشی شارژ نمی‌شود
- **راه‌حل:** گوشی را به شارژر دیواری وصل کنید
- انتقال داده از طریق Wi-Fi انجام می‌شود

### 📶 پایداری اتصال
- گوشی و سرور باید در یک شبکه باشند
- از Wi-Fi پایدار استفاده کنید (نه Mobile Hotspot)
- برای اتصال مجدد: `adb connect IP:5555`
- برای قطع اتصال: `adb disconnect IP:5555`

---

## 🎯 خلاصه دستورات

| دستور | توضیح |
|-------|-------|
| `adb devices` | لیست دستگاه‌های متصل |
| `adb tcpip 5555` | فعال‌سازی حالت TCP/IP |
| `adb connect IP:5555` | اتصال بی‌سیم |
| `adb disconnect IP:5555` | قطع اتصال |

---

## ✅ چک‌لیست آماده‌سازی

- [ ] USB Debugging فعال شده
- [ ] گوشی به Wi-Fi متصل است
- [ ] IP گوشی یادداشت شده
- [ ] ADB Platform Tools دانلود شده
- [ ] دستور `adb tcpip 5555` اجرا شده
- [ ] اتصال در VM تست شده

---

**موفق باشید! 🚀**